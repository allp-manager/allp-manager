# Snap Fixtures

These fixtures cover parser behavior for Snap metadata validation:

- decorated verified publishers;
- plain publishers;
- canonical names and display titles;
- classic and strict confinement;
- stable, candidate, beta, and edge channels;
- edge-only packages with no stable channel;
- installed-state parsing from `snap list`.
